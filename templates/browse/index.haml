-@title = "All Servers"
%h1=@title

.grep=haml(:grep_form, :layout => false)
%p Warning, grepping all logs will be very slow, it's better to browse in a bit first.

%h2 Browse
%ul
  -@log_list.each do |server|
    %li
      %a{:href => "/browse/#{escape(server.name)}"}=server.name